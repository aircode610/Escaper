# Env and API keys
python-dotenv>=1.0.0

# ScrapFly + parsing
scrapfly-sdk[concurrency]>=0.8.24
beautifulsoup4>=4.12.0

# Agent (LangGraph + LLM)
langgraph>=0.2.0
langchain-anthropic>=0.3.0
langchain-core>=0.3.0
langsmith>=0.2.0
pydantic>=2.0
